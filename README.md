# Wyre Frontend

## Setup

1. Install dependencies: `npm i`

2. Run locally: `npm run dev`

3. Deploy to Vercel: `vercel deploy`

## Structure

- Landing page at `/`

- Dashboard at `/dashboard`

Uses Vite, React, Tailwind CSS, with dark mode toggle.

API integrated with https://records.anchorvalley.us endpoints.

